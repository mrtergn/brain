# Cache Layout

This cache is generated from project scans and embedding runs.

- `projects/` stores normalized project snapshots.
- `chunks/` stores the last embedded chunk payload per project.

Delete cache contents freely when you want a clean rebuild.