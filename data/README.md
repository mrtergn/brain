# Runtime Data

This directory is local runtime state, not source code.

Tracked here:

- README placeholders that explain the runtime layout.

Generated here:

- cache snapshots
- chunk cache
- Chroma data
- logs
- state files
- helper launchers in `data/runtime`

If the runtime gets messy, prefer rerunning the workflow over manually curating files.