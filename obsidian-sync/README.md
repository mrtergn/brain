# Obsidian Sync Sandbox

Local sandbox vault for self-test or validation runs.

The primary live vault is whatever `brain.config.json`, environment variables, or CLI flags resolve as the active vault root.

Do not place executable code here.